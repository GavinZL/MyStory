

要求：
1. 工程中组件公用 [在需要实现新的组件是，先看看工程中是否存在对应组件]
2. 工程文件处理中，需要遵守基本的设计原则，SOLID原则， 可以参考 @StoryEditorView.swift的组织方式，
    大体包括：
        MVVM 架构：视图与业务逻辑分离
        组合模式：大视图拆分为小组件
        单一职责：每个方法只做一件事
        依赖注入：通过初始化器注入依赖