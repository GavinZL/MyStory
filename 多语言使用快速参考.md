# MyStory 多语言使用快速参考

## 🚀 快速开始

### 在代码中使用本地化文本

#### 方式 1: 基础用法
```swift
Text("common.cancel".localized)
Button("common.save".localized) { }
.navigationTitle("timeline.title".localized)
```

#### 方式 2: 带参数
```swift
// 单个参数
Text(String(format: "category.storyCount".localized, count))

// 多个参数
Text(String(format: "message.format".localized, name, count))
```

#### 方式 3: 便捷属性
```swift
Text(String.cancel)  // "取消" / "Cancel"
Text(String.save)    // "保存" / "Save"
```

## 📋 常用本地化键值

### 通用按钮
```swift
"common.cancel"     // 取消 / Cancel
"common.save"       // 保存 / Save
"common.done"       // 完成 / Done
"common.delete"     // 删除 / Delete
"common.edit"       // 编辑 / Edit
"common.add"        // 添加 / Add
"common.confirm"    // 确定 / OK
```

### Tab 栏
```swift
"tab.timeline"      // 时间轴 / Timeline
"tab.category"      // 分类 / Categories
"tab.settings"      // 设置 / Settings
```

### 故事编辑器
```swift
"story.new"         // 新建故事 / New Story
"story.edit"        // 编辑故事 / Edit Story
"story.title"       // 标题 / Title
"story.content"     // 内容 / Content
"story.media"       // 媒体 / Media
"story.location"    // 位置 / Location
```

### 分类管理
```swift
"category.title"    // 分类 / Categories
"category.new"      // 新建分类 / New Category
"category.level1"   // 一级分类 / Level 1
"category.level2"   // 二级分类 / Level 2
"category.level3"   // 三级分类（故事线） / Level 3 (Storyline)
```

### 设置
```swift
"settings.title"    // 设置 / Settings
"settings.language" // 语言 / Language
"settings.theme"    // 主题 / Theme
```

## 💡 最佳实践

### 1. 新增文本时
```swift
// ❌ 错误 - 硬编码
Text("提交")

// ✅ 正确 - 使用本地化
Text("common.submit".localized)
```

### 2. 动态内容
```swift
// ✅ 使用 String.format
let message = String(format: "category.storyCount".localized, 5)
// 中文: "共 5 个故事"
// English: "5 stories"
```

### 3. 日期格式
```swift
let formatter = DateFormatter()
formatter.dateFormat = "timeline.dateFormat".localized
// 中文: "yyyy年MM月dd日 HH:mm"
// English: "MMM dd, yyyy HH:mm"
```

### 4. 空状态提示
```swift
VStack {
    Image(systemName: "tray")
    Text("categoryStory.empty".localized)
    Button("categoryStory.createFirst".localized) { }
}
```

## 🔧 添加新的本地化文本

### 步骤 1: 确定键值名称
遵循命名规范：`模块.具体含义`

例如：
- `profile.title` - 个人资料标题
- `profile.edit` - 编辑个人资料
- `profile.save` - 保存个人资料

### 步骤 2: 添加到资源文件

**zh-Hans.lproj/Localizable.strings:**
```
"profile.title" = "个人资料";
"profile.edit" = "编辑资料";
"profile.save" = "保存";
```

**en.lproj/Localizable.strings:**
```
"profile.title" = "Profile";
"profile.edit" = "Edit Profile";
"profile.save" = "Save";
```

### 步骤 3: 在代码中使用
```swift
Text("profile.title".localized)
```

### 步骤 4: （可选）添加便捷属性

**String+Localization.swift:**
```swift
extension String {
    static let profileTitle = "profile.title".localized
    static let profileEdit = "profile.edit".localized
}
```

使用：
```swift
Text(String.profileTitle)
```

## 🌐 语言切换

### 用户操作流程
1. 打开应用
2. 进入"设置" Tab
3. 点击"语言"
4. 选择想要的语言
5. 点击"完成"
6. 界面立即更新

### 编程方式切换
```swift
LocalizationManager.shared.setLanguage(.chinese)  // 切换到中文
LocalizationManager.shared.setLanguage(.english)  // 切换到英文
```

### 获取当前语言
```swift
let current = LocalizationManager.shared.currentLanguage
// .chinese 或 .english
```

## 📝 资源文件组织

```
Resources/Localizable/
├── zh-Hans.lproj/
│   ├── Localizable.strings    # 中文应用内文本
│   └── InfoPlist.strings      # 中文权限描述
└── en.lproj/
    ├── Localizable.strings    # 英文应用内文本
    └── InfoPlist.strings      # 英文权限描述
```

## ⚡ 性能优化

### 1. 复用本地化字符串
```swift
// ❌ 每次都重新获取
Text("common.cancel".localized)
Text("common.cancel".localized)

// ✅ 缓存后复用
let cancelText = "common.cancel".localized
Text(cancelText)
Text(cancelText)
```

### 2. 使用便捷属性
```swift
// 预定义常用文本
static let cancel = "common.cancel".localized
static let save = "common.save".localized
```

## 🐛 常见问题

### Q: 文本没有显示本地化？
**A:** 检查：
1. 键值是否存在于两个语言文件中
2. 拼写是否正确
3. 是否调用了 `.localized`
4. Xcode 项目中是否正确添加了资源文件

### Q: 切换语言后部分文本没更新？
**A:** 
- SwiftUI 视图应自动更新
- 如果使用 `@StateObject` 或 `@ObservedObject`，确保订阅了 `LocalizationManager`
- 某些缓存的文本可能需要手动刷新

### Q: 如何测试不同语言？
**A:** 
1. 方式 1: 在应用内切换语言
2. 方式 2: 修改模拟器/设备系统语言
3. 方式 3: Xcode Scheme 设置应用语言

### Q: 日期格式不对？
**A:** 
确保使用本地化的日期格式：
```swift
formatter.dateFormat = "timeline.dateFormat".localized
```

## 📚 完整键值列表

查看详细的键值列表，请参考：
- `Resources/Localizable/zh-Hans.lproj/Localizable.strings`
- `Resources/Localizable/en.lproj/Localizable.strings`

或查看完整检查报告：
- `多语言支持完整检查报告.md`

## 🎯 记住这些要点

1. **所有用户可见文本都要本地化**
2. **两个语言文件保持键值同步**
3. **使用有意义的键值命名**
4. **参数化字符串用 String.format**
5. **日期时间使用本地化格式**
6. **新增文本同时更新两个文件**

---

**快速查询**: 按 `Cmd + F` 搜索关键词
**更新时间**: 2025-12-03
